require 'spec_helper'

describe Rack::Vcr do
  it 'has a version number' do
    expect(Rack::Vcr::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
